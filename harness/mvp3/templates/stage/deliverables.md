# Stage NN — Deliverables

이 단계가 완료되면 다음 산출물이 존재해야 한다.

## 새로 생성되는 파일
- (예: `eactive-resource-hub/build.gradle`)

## 새로 생성되는 패키지/디렉토리
- (예: `com.eactive.resourcehub.user`)

## 새로 생성되는 DB 객체
- (예: Flyway V1 마이그레이션 + users 테이블)

## 새로 생성되는 엔드포인트
- (예: `GET /health`, `POST /signup`)
